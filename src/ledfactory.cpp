#include "ledfactory.h"

LEDFactory::LEDFactory()
    : lastCreatedLedIndex(-1)
{
}

void LEDFactory::createLED(const QString& name)
{
//    LED led(name);
//    ledList_.append(led);
//    lastCreatedLedIndex_ = ledList_.size() - 1;
    //MainWindow::GUI_CTL_LEDS(ledList_.size()).setText(led.getName());
}

void LEDFactory::setGuiLedColor()
{
//    for (int i = 0; i < ledList_.size(); i++) {
//        if (i < MainWindow::GUI_CTL_LEDS.count()) {
//            LED led = ledList_.at(i);
//            if (led.isOn()) {
//                //MainWindow::GUI_CTL_LEDS(i + 1).setBorderColor(Color::Lime);
//            } else {
//                //MainWindow::GUI_CTL_LEDS(i + 1).setBorderColor(Color::Gainsboro);
//            }
//        }
//    }
}
